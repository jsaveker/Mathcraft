{
  "name": "mathcraft",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A playful 3D addition and subtraction adventure.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "format": "prettier --write src tests scripts \"docs/**/*.md\" index.html vite.config.js README.md",
    "format:check": "prettier --check src tests scripts \"docs/**/*.md\" index.html vite.config.js README.md",
    "build:website": "vite build --base=/mathcraft/",
    "export:website": "node scripts/export-website.mjs"
  },
  "dependencies": {
    "@fontsource-variable/dm-sans": "^5.3.0",
    "@fontsource-variable/outfit": "^5.3.0",
    "three": "^0.183.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "vite": "^7.3.1"
  }
}
